create a new storyboard containing the following points:

1) Clean the docker-compose.yml file. It represents only the production environment. The development is done starting Backend/Frontend from the IDE and connecting to the database in docker.
2) Review, cleaning and refactoring. Clean Clippy warnings.
3) Use the Testing to prepare a playwright E2E testing.
4) Integrate the IAM: https://github.com/ferriskey/ferriskey
5) Change the flow from that for the order now the user need to be registered and authenticated with feriskey (JWT).
6) Store the order with history in the database.
7) Update architecture.md and business.md